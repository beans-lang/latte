module board
kind application

# The demo needs all four: latte for the component model, latte_app for the
# composition root, barista because it names ServiceCollection itself, and
# espresso because `check` drives the app through a TestHost.
require path "../.."
require path "../../app"
require github.com/beans-lang/barista v0.1.1
require github.com/beans-lang/espresso v0.3.0
