module p12_consumer
kind application

# The latte module root sits two directories above this manifest, from BOTH
# `community-libs/latte/probes/p12_consumer_type_of` and
# `.worktrees/latte-wN/probes/p12_consumer_type_of`. A relative `require path`
# resolves against the requiring manifest, so one row serves both.
require path "../.."
